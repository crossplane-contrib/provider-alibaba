apiVersion: sls.alibaba.crossplane.io/v1alpha1
kind: SLSProject
metadata:
  name: sls-project-test
  namespace: default
spec:
  forProvider:
    name: project-test
    description: provisioned by Crossplane
  writeConnectionSecretToRef:
    name: sls-endpoint
    namespace: default
