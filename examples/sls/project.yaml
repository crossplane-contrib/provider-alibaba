apiVersion: sls.alibaba.crossplane.io/v1alpha1
kind: Project
metadata:
  name: sls-project-test
  namespace: default
spec:
  forProvider:
    name: sls-project-sample
    description: provisioned by Crossplane
  writeConnectionSecretToRef:
    name: sls-endpoint
    namespace: default
